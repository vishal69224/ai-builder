/**
 * @deprecated Sidebar removed — navigation lives in DashboardLayout top bar.
 * Kept as a no-op export so old imports don’t break mid-reload.
 */
export function Sidebar(_props: { open: boolean; onClose: () => void }) {
  return null
}
